spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zhishu_test_f07?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=utf-8
    username: root
    password: ZhaoYang1314.
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  data:
    redis:
      host: 192.168.241.128
      port: 6379
      password: ZhaoYang1314.
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: file-processing-group
      auto-offset-reset: earliest
      properties:
        spring.json.trusted.packages: "*"
    topic:
      file-processing: file-processing-topic1
      dlt: file-processing-dlt

admin:
  bootstrap:
    enabled: false

knowledge:
  bootstrap:
    enabled: false

jwt:
  secret-key: bXlfc2VjdXJlX2tleV8xMjM0NTY3ODkwMTIzNDU2Nzg5MDEyMzQ1Njc=

model-provider:
  security:
    secret-key: YWFhYWFhYWFhYWFhYWFhYWFhYWFhYWFhYWFhYWFhYWE=

minio:
  endpoint: http://localhost:9000
  accessKey: minioadmin
  secretKey: minioadmin
  bucketName: test-bucket
  publicUrl: http://localhost:9000

file:
  parsing:
    chunk-size: 512
    parent-chunk-size: 1048576
    buffer-size: 8192
    max-memory-threshold: 0.8

elasticsearch:
  host: localhost
  port: 9200
  scheme: http
  username: elastic
  password: changeme
  insecure-trust-all-certificates: true
  init:
    enabled: false

security:
  allowed-origins: http://localhost:3000

logging:
  level:
    org.springframework: WARN
    com.canggo.zhishu: INFO
    org.hibernate.SQL: WARN

deepseek:
  api:
    url: http://localhost:11434/v1
    model: deepseek-chat
    key:

embedding:
  api:
    url: http://localhost:8000
    key:
    model: text-embedding-v4
